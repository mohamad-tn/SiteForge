SiteForge STATUS
Updated: 2026-09-09 18:49 Asia/Damascus (UTC+3)
Dev: http://localhost:3000
Tenant: demo@siteforge.local / demo1234
Admin: admin@siteforge.local / admin1234 (/admin)
Public: http://localhost:3000/s/demo-studio (published only)

Readiness: ~9.2 / 10 editor chrome + motion polish (NOT Framer freeform canvas)


## Cleanup — stagger depth + canvas empty i18n (2026-09-09)

### Shipped
1) Stagger CSS depth-limited to outermost grid/flex/stack under `.sf-stagger` (nested `flex-col`/`space-y` inside cards no longer cascade)
2) Canvas empty card uses `canvasEmptyTitle` / `canvasEmptyBody` via `usePlatformLangOptional` (not tokens.rtl hardcode)
3) `staggerHelp` AR/EN copy mentions one-level / no nested stacks

### QA
- vitest targeted + tsc --noEmit
- next build skipped (:3000 busy)



## Wave 3 — stagger widen + mobile Site shortcuts + easing (2026-09-09)

### Shipped
1) Child stagger now targets grids AND common flex/stack containers (`space-y-*`, `flex-wrap`, `flex-col`) — reduced-motion still kills animations
2) Mobile Site shortcuts: compact SEO/Domain/Secrets icon row always visible below md; clearer overflow section labels
3) Optional easing preset (ease-out / springy / soft) → `--sf-ease` CSS; one clean inspector select
4) Visual polish: insert palette i18n + hover, layers empty CTA, inspector no-selection, canvas empty card

### Gaps still open
- Still not Framer freeform canvas / timeline editor
- next build skipped (:3000 busy with next-server)


## Pass focus — Seam collapse + templates + تأثيرات (2026-09-09)

### 1) Collapse UX (Figma/Linear seam)
- Removed misplaced collapse from header, inspector header, «…» menu, and floating top-corner restore buttons
- Unified affordance: slim seam pills centered on the panel↔canvas edge (xl+)
- When collapsed: consistent vertical rail/edge tab re-opens the panel
- Prefs still in localStorage (sf-editor-left-collapsed / sf-editor-right-collapsed)

### 2) Design depth + Selects
- Deeper sf-panel chrome (refined borders/shadows, dark-mode contrast)
- Content language in editor header → polished Radix Select (no native <select>)
- Theme/font Selects in properties use denser trigger styling + AR/EN theme labels
- Canvas inset polish

### 3) Ready-made templates (GitHub-marketing aesthetics)
- Rebuilt CV / Portfolio / Landing / Business / Restaurant in prisma/seed.ts
- Stronger palettes (deep blacks, teal/violet/amber/orange accents), spacing, hero copy
- Cohesive navbar → hero → features/gallery → footer; light effect presets on key sections
- Thumbnail SVGs refreshed under public/templates/*.svg
- DB re-seeded via `pnpm db:seed` (demo-studio uses upgraded portfolio)

### 4) Site effects — تأثيرات (friendly)
- Presets: بدون / تلاشي ناعم / انزلاق لأعلى / تكبير تدريجي / طفو / توهج عند التمرير / رفع عند التمرير / ظهور من ضباب
  (none, soft-fade, slide-up, scale-in, float, glow-hover, lift-hover, blur-in)
- Optional «ظهور عند التمرير» (IntersectionObserver) on published/public view
- Whole-block scope clarified when a text part is selected
- prefers-reduced-motion respected (CSS + IO short-circuit)
- Inspector tab renamed to تأثيرات / Effects with plain help text

### 5) GitHub push
- NOT handled this pass (deferred to parent)

### QA
- vitest: 52 passed (incl. effect preset mapping)
- tsc --noEmit: clean
- Do NOT run next build while pnpm dev on :3000

### How to test (AR)
1) سجّل demo@siteforge.local / demo1234 → افتح محرر demo-studio
2) على شاشة واسعة: اطوِ اللوحات من حبوب الحافة وسط الحد بين اللوحة والكانفس — ليس من الهيدر
3) عند الطي: شريط جانبي رفيع يعيد الفتح؛ التفضيل يبقى بعد إعادة التحميل
4) لغة المحتوى في الشريط العلوي = قائمة Radix أنيقة (وليست قائمة أصلية)
5) الخصائص → تأثيرات: اختر مسبقاً جاهزاً + «عند التمرير للعنصر» ثم انشر وجرّب /s/demo-studio
6) لوحة التحكم → قوالب جديدة: سيرة / معرض / هبوط / أعمال / مطعم بمظهر أعمق
